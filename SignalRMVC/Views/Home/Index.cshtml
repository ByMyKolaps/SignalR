@model IEnumerable<Chat>
@{
    ViewData["Title"] = "Home Page";
}

<div class="chat-body">
    @foreach (var chat in Model)
    {
        <a asp-controller="Home" asp-action="JoinRoom" asp-route-id="@chat.Id" class="room-button">
            @chat.Name
        </a>
    }
</div>
